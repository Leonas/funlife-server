development:
  adapter: postgresql
  host: localhost
  username: user
  database: funlife-development

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: postgresql
  host: localhost
  username: user
  database: funlife-test

#circleCI info
#Both postgres and mysql are set up to use the ubuntu user,
#have a database called circle_test available,
#and don't require any password.

production:
  adapter: postgresql
  host: localhost
  username: user
  database: funlife-production